ector<int> Solution::maxset(vector<int> &A) {
     while(i<A.size())
     { 
         int leng=0;
         int sum=0;
         while(A[i]>=0)
         {sum+=A[i];
             
         }
     }
  

}
