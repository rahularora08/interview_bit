 int low=0;
    int high=m-1;
    while(low <= high)
    {
        int mid=(low+high)/2;
        if(B==A[mid][0])
         return 1;
        else if(B>A[mid][0])
         low=mid+1;
        else
         high=mid-1;
    }
    return high;
